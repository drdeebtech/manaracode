# Node
frontend/node_modules/
frontend/dist/

# Go build output
backend/server
backend/manaracode-backend

# Vite dev cache
.vite/

# SQLite (local dev stray files — production DB lives in a named Docker volume)
*.db
backend/data/

# CDK infrastructure
infrastructure/node_modules/
infrastructure/cdk.out/
infrastructure/.claude-flow/

# Misc
.DS_Store
.env
.gbrain-source
.gstack/

# Local agent/skill installs (regenerated per machine; gstack submodule is the
# tracked shared copy, Supabase skills reproduce from skills-lock.json)
.agents/
.sources/
.claude/skills/*
!.claude/skills/gstack
.claude/scheduled_tasks.lock

# Vendored third-party tooling (kept local, not shipped)
.claude/skills/gstack/
